{
  "name": "@obsidianhub/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "pretest": "corepack pnpm --filter @obsidianhub/core build && corepack pnpm --filter @obsidianhub/vault build && corepack pnpm --filter @obsidianhub/actions build",
    "test": "node --experimental-strip-types --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "pretest": "corepack pnpm --filter @obsidianhub/actions build",
    "test": "node --experimental-strip-types --test test/**/*.test.ts",
    "clean": "rm -rf .next"
  },
  "dependencies": {
    "@obsidianhub/actions": "workspace:*",
    "@obsidianhub/config": "workspace:*",
    "@obsidianhub/core": "workspace:*",
    "@obsidianhub/vault": "workspace:*",
    "next": "^15.2.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4"
  }
}
